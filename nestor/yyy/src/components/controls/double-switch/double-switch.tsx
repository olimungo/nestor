import styles from './double-switch.module.css';

type Props = {};

export function DoubleSwitch(props: Props) {
    // const {} = props;

    return (
        <div className={`${styles.component}`}>
            <div>DOUBLE-SWITCH</div>
        </div>
    );
}
