import styles from './stepper-h.module.css';

type Props = {};

export function StepperH(props: Props) {
    // const {} = props;

    return (
        <div className={`${styles.component}`}>
            <div>STEPPER-H</div>
        </div>
    );
}
