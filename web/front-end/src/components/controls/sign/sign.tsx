import styles from './sign.module.css';

type Props = {};

export function Sign(props: Props) {
    // const {} = props;

    return (
        <div className={`${styles.component}`}>
            <div>SIGN</div>
        </div>
    );
}
