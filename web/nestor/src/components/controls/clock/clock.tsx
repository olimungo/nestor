import styles from './clock.module.css';

type Props = {};

export function Clock(props: Props) {
    // const {} = props;

    return (
        <div className={`${styles.component}`}>
            <div>SLIDER</div>
        </div>
    );
}
