import styles from './stepper-v.module.css';

type Props = {};

export function StepperV(props: Props) {
    // const {} = props;

    return (
        <div className={`${styles.component}`}>
            <div>STEPPER-V</div>
        </div>
    );
}
